 - check for 'over generous' file permissions if we have ftp/password in
   config
 - allow rsync with username (that differs from locla machine)
 - use Path::Class for building and manipulating paths
